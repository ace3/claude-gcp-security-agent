# ─── Scan outputs (all findings stay local, never committed) ─────────────────
scan-output/

# ─── GCP credentials & key files ─────────────────────────────────────────────
*.json
!schemas/*.json          # keep schema definitions
!schemas/firewall-finding.schema.json
!schemas/firewall-phase-state.schema.json
gcp-*.key
*.p12
*.pem
service-account*.json
credentials*.json

# ─── Local project config (copy config.example.env → config.local.env) ───────
config.local.env
config.local.md
.env
.env.*
!.env.example

# ─── macOS artifacts ──────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ─── Editor / IDE ─────────────────────────────────────────────────────────────
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~
